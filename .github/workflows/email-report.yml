name: email 

on:
  schedule:
    - cron: '00 9 * * 1,4'
    
env:
    REPORT_DIR: /data/watchtower/ctx-API/container-results/api-exposure

jobs:
    send-email:
        runs-on: "th878"
        steps:
        - name: Email latest report
          uses: USEPA/email-report@main
          with:
                report_path: ${{ env.REPORT_DIR }}/exposure-report.html
                recipients: ${{ vars.REPORT_RECIPIENTS }}
                sender: ${{ vars.REPORT_SENDER }}
                subject: "CTX Exposure API Scan Report"
                body: |
                    Hello,

                    Please find attached the CTX Exposure API scan report.

                    Best regards.